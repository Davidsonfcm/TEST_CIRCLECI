version: 2
jobs:
  build:
    docker:
      - image: alpine:3.7

    steps:
      - checkout

      - run:
          name: Instalar dependências
          command: npm install

      - run:
          name: Executar test
          command: npm test
