version: 2
jobs:
  build:
    docker:
      - image: alpine:3.7

    steps:
      - checkout

      - run:
          name: Instalar NODE e NPM
          command: apk add --update nodejs nodejs-npm

      - run:
          name: Instalar dependências
          command: npm install

      - run:
          name: Executar test
          command: npm test
